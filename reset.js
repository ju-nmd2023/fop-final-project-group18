export function resetGame() {
  score = 0;

  // Resets the cars position to default
  singlePlayer.x = innerWidth / 2;
  player1.x = middleWidth / 2;
  player2.x = middleWidth * 1.5;

  // Resets the traffic cars

  // Reset the grass
}
